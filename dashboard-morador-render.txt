            }`}
            painel="/dashboard/morador"
            titulo="QR, placa e links da residÃªncia"
          />
        )}
              </>
            )}
          </div>
        </div>
      </DashboardBase>
    );
  }

  /*
   * =====================================================
   * CONDOMINIO
   * PAINEL DO MORADOR
   * =====================================================
   */

  return (
    <DashboardBase>
      <div className="grid gap-5 lg:grid-cols-[220px_minmax(0,1fr)]">

        <aside className="rounded-3xl border border-slate-800 bg-slate-900 p-4 lg:min-h-[calc(100vh-7rem)]">
          <div className="mb-6">
            <p className="text-xs font-black uppercase tracking-[0.16em] text-cyan-400">
              QR CondomÃ­nio
            </p>

            <p className="mt-2 text-xl font-black text-white">
              {nomeLocal}
            </p>

            {unidadeId && (
              <p className="mt-1 text-xs font-bold text-slate-400">
                Minha unidade
              </p>
            )}
          </div>

          <nav className="space-y-2">
            <button
              type="button"
              className="w-full rounded-2xl bg-cyan-500 px-4 py-3 text-left text-sm font-black text-slate-950"
            >
              InÃ­cio
            </button>

            <button
              type="button"
              onClick={abrirAtendimentoVisitantes}
              className="w-full rounded-2xl border border-slate-700 bg-slate-800 px-4 py-3 text-left text-sm font-bold text-slate-100 transition hover:border-cyan-500 hover:bg-slate-700"
            >
              Atendimento de visitantes
            </button>

            <button
              type="button"
              onClick={() =>
                alert(
                  "Acessos e visitantes da unidade serÃ£o conectados aqui."
                )
              }
              className="w-full rounded-2xl border border-slate-700 bg-slate-800 px-4 py-3 text-left text-sm font-bold text-slate-100 transition hover:border-cyan-500 hover:bg-slate-700"
            >
              Acessos / Visitantes
            </button>

            <button
              type="button"
              onClick={() =>
                alert(
                  "Comunicados do condomÃ­nio serÃ£o conectados aqui."
                )
              }
              className="w-full rounded-2xl border border-slate-700 bg-slate-800 px-4 py-3 text-left text-sm font-bold text-slate-100 transition hover:border-cyan-500 hover:bg-slate-700"
            >
              Comunicados
            </button>

            <button
              type="button"
              onClick={() =>
                alert(
                  "ConfiguraÃ§Ãµes do morador serÃ£o conectadas aqui."
                )
              }
              className="w-full rounded-2xl border border-slate-700 bg-slate-800 px-4 py-3 text-left text-sm font-bold text-slate-100 transition hover:border-cyan-500 hover:bg-slate-700"
            >
              ConfiguraÃ§Ãµes
            </button>
          </nav>
        </aside>

        <div className="min-w-0">

          <section className="rounded-3xl bg-gradient-to-r from-blue-700 via-blue-600 to-cyan-600 px-7 py-7 text-white shadow-xl">
            <div className="flex flex-col gap-5 xl:flex-row xl:items-center xl:justify-between">
              <div className="min-w-0">
                <p className="text-sm font-black text-blue-100">
                  {nomeLocal}
                </p>

                <h1 className="mt-1 text-3xl font-black md:text-4xl">
                  Bom dia,{" "}
                  {usuario.nome?.split(" ")[0] || "Morador"}
                </h1>

                <p className="mt-1 text-sm text-blue-100 md:text-base">
                  Sua unidade e os serviÃ§os do condomÃ­nio em um sÃ³ lugar.
                </p>
              </div>

              <div className="flex flex-wrap items-stretch gap-2">
                <div className="min-w-[220px] rounded-2xl border border-white/30 bg-white/10 px-4 py-3">
                  <p className="text-[10px] font-black uppercase tracking-wider text-blue-100">
                    Perfil
                  </p>

                  <p className="mt-1 text-sm font-black text-white">
                    Morador
                  </p>
                </div>

                <button
                  type="button"
                  onClick={sair}
                  disabled={saindo}
                  className="rounded-2xl border border-red-300/50 bg-red-600 px-5 py-3 text-sm font-black text-white transition hover:bg-red-500 active:scale-[0.98] disabled:opacity-60"
                >
                  {saindo ? "Saindo..." : "Sair"}
                </button>
              </div>
            </div>

            <div className="mt-5 rounded-2xl border border-white/20 bg-black/10 p-4">
              <p className="text-xs font-bold uppercase tracking-wider text-blue-100">
                Minha unidade
              </p>

              <p className="mt-1 text-lg font-black">
                {unidadeId || "Unidade vinculada"}
              </p>
            </div>
          </section>

          <section className="mt-5 rounded-3xl border border-slate-800 bg-slate-900 p-5 md:p-6">
            <p className="text-xs font-black uppercase tracking-wider text-cyan-400">
              Meus acessos
            </p>

            <h2 className="mt-1 text-2xl font-black text-white">
              Acesso rÃ¡pido
            </h2>

            <p className="mt-2 text-sm text-slate-400">
              Aqui aparecem somente os recursos liberados para o seu perfil e sua unidade.
            </p>

            <div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">

              {podeReceberChamadas && (
                <button
                  type="button"
                  onClick={abrirAtendimentoVisitantes}
                  className="rounded-2xl border border-blue-800 bg-blue-950/40 p-4 text-left transition hover:border-blue-500 hover:bg-blue-950 active:scale-[0.98]"
                >
                  <div className="text-3xl">
                    ðŸ””
                  </div>

                  <div className="mt-3 font-black text-white">
                    Atendimento de visitantes
                  </div>

                  <div className="mt-1 text-sm text-slate-400">
                    Receba chamadas destinadas Ã  sua unidade.
                  </div>
                </button>
              )}

              {podeAbrirPortao && (
                <button
                  type="button"
                  onClick={abrirPortao}
                  disabled={abrindoPortao}
                  className="rounded-2xl border border-violet-800 bg-violet-950/40 p-4 text-left transition hover:border-violet-500 hover:bg-violet-950 active:scale-[0.98] disabled:opacity-60"
                >
                  <div className="text-3xl">
                    ðŸšª
                  </div>

                  <div className="mt-3 font-black text-white">
                    Abrir portÃ£o
                  </div>

                  <div className="mt-1 text-sm text-slate-400">
                    Acione o portÃ£o autorizado do condomÃ­nio.
                  </div>
                </button>
              )}

              {podeVisualizarCameras && (
                <div className="rounded-2xl border border-cyan-800 bg-cyan-950/30 p-4">
                  <div className="text-3xl">
                    ðŸ“·
                  </div>

                  <div className="mt-3 font-black text-white">
                    CÃ¢meras
                  </div>

                  <div className="mt-1 text-sm text-slate-400">
                    Visualize somente as cÃ¢meras liberadas para seu perfil.
                  </div>
                </div>
              )}

              {podeControlarAlarme && (
                <div className="rounded-2xl border border-emerald-800 bg-emerald-950/30 p-4">
                  <div className="text-3xl">
                    ðŸš¨
                  </div>

                  <div className="mt-3 font-black text-white">
                    Alarme
                  </div>

                  <div className="mt-1 text-sm text-slate-400">
                    Controle disponÃ­vel conforme autorizaÃ§Ã£o do condomÃ­nio.
                  </div>
                </div>
              )}
            </div>

            {!podeReceberChamadas &&
              !podeAbrirPortao &&
              !podeVisualizarCameras &&
              !podeControlarAlarme && (
                <div className="mt-5 rounded-2xl border border-dashed border-slate-700 p-8 text-center">
                  <p className="font-black text-white">
                    Nenhum acesso liberado
                  </p>

                  <p className="mt-2 text-sm text-slate-500">
                    O condomÃ­nio ainda nÃ£o liberou recursos para este perfil.
                  </p>
                </div>
              )}

            {statusPortao && (
              <p className="mt-4 rounded-2xl border border-slate-700 bg-slate-950 px-4 py-3 text-sm font-black text-white">
                {statusPortao}
              </p>
            )}
          </section>

        </div>
      </div>
